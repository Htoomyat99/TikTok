import styles from '../../../styles/Home.module.scss';

const Upload = () => {   
    return (
        <div className={styles.container}>
            <h3>this is upload page</h3>  
        </div>
    );
};

export default Upload;